// scripts.js

function getTriangleArea(a, h) {
	if (a,h > 0) {
    		return (a*h/2) 
	} else if (value < 0) { 
		console.log('Nieprawidłowe dane')
	} else{ 
		console.log('Nieprawidłowe dane')};
};
console.log( getTriangleArea(10,6) );
